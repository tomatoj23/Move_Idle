{
  "name": "move-idle-validator",
  "version": "0.1.0",
  "private": true,
  "description": "Move_Idle content library validator: JSON Schema (draft 2020-12) + reference integrity + id rules. Single source of truth shared by CLI and editor (#26/#10/#24).",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "validate": "node src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
